- [ ] Implement ADO.NET + SQL Server persistence for WeatherRepository using ConnectionStrings:DefaultConnection
- [ ] Add model/table creation strategy (assume table dbo.Weather with Date(int?) Temp(int) Summary(nvarchar))
- [ ] Update WeatherRepository to use SqlConnection/SqlCommand and persist GetAll/Add
- [ ] Ensure dependency injection remains correct
- [ ] Run dotnet build and a quick manual smoke test via controller endpoints

